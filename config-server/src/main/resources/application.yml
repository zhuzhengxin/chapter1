spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://rdc.hand-china.com/gitlab/15433/config1314.git
          username:
          password:
          searchPaths: '{profile}'
server:
  port: 8888
